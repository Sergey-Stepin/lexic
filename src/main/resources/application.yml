server:
  port: 9090

logging:
  level:
    sql: DEBUG

spring:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create
    show-sql: false
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:drone_database
    username: sa
    password: